description: Successfully found contacts or contact by id
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: SuccessfullyFoundContacts
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Successfully found contacts'